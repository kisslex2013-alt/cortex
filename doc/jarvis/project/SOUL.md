# 🧠 SOUL.md — Memory & Soul

## Философия

Память Jarvis — это не одна база, а **управляемый слой**: что запомнить, где хранить, как использовать и когда забыть.

## Принципы

1. **Контролируемость** — пользователь может просмотреть, исправить, удалить, экспортировать любой факт
2. **Минимизация** — хранить "суть и выводы", не сырые логи
3. **Безопасность** — секреты **никогда** в памяти (только ссылки на Vault)
4. **Объяснимость** — если Jarvis использует факт — он показывает источник
5. **Забывание** — каждый факт имеет TTL, периодическая очистка

## Архитектура памяти

### Три уровня

```
┌────────────────────┐
│ FAST MEMORY (RAM)  │  ← Текущий диалог, рабочий контекст
│ TTL: сессия        │  ← SQLite in-memory / Map()
│ Размер: ≤50KB      │
├────────────────────┤
│ LONG MEMORY        │  ← Факты, предпочтения, знания, проекты
│ TTL: настраиваемый │  ← SQLite file + FTS5
│ Категории:         │
│  personal, tech,   │
│  financial,        │
│  incident,         │
│  directive         │
├────────────────────┤
│ VECTOR MEMORY      │  ← Семантический поиск
│ TTL: ∞             │  ← ChromaDB (embeddings)
│ Источники: docs,   │
│  skills, logs,     │
│  conversation      │
└────────────────────┘
```

### RAG Pipeline

```
WRITE:  Факт → Categorize → Extract entities → Embed → Store (Long + Vector)
READ:   Query → Embed → Semantic search (Vector, top-5) → FTS search (Long) → Merge → Inject context
FORGET: TTL check → Archive cold facts → Delete expired → Compact DB
```

**Ключевое:** вместо context stuffing (2M токенов) → RAG (400–500 токенов релевантного контекста). Экономия ~75% токенов.

### Дедупликация

Перед INSERT: FTS5 search(new_fact, limit=3). Если similarity ≥ 0.85 → merge (обновить timestamp + access_count). Если < 0.85 → INSERT новый факт.

### Confidence Scoring

(Вдохновлено OpenAI Advanced Memory)

| Уровень | Источник | Пример |
|---------|---------|--------|
| **high** | Подтверждено инструментом (exec/API/файл) | "Node v22.14 установлен" |
| **medium** | Сказано пользователем | "Мне нравится TypeScript" |
| **low** | Выведено LLM | "Вероятно, предпочитает dark mode" |

При поиске: `ORDER BY confidence DESC, relevance DESC`.

### Memory GC (Garbage Collection)

```
Cron каждые 24ч:
  1. DELETE expired: WHERE expires_at < NOW()
  2. Archive cold: INSERT INTO archive SELECT * WHERE access_count=0 AND age>90d
  3. Compact DB: VACUUM
  4. Log: GC report (deleted, archived, db_size)
```

## Контекстные зоны

| Зона | Температура | Пример | Действие |
|------|------------|--------|----------|
| **HOT** | Активный диалог | Текущий разговор | В промпте |
| **WARM** | Недавно использовано | Последние 24ч фактов | RAG top-5 |
| **COLD** | Давно не использовано | Старые факты (>30 дней) | Только по запросу |
| **ARCHIVE** | Архив | >90 дней | Сжатый backup |

**Источник:** [JARVIS_LEAN_AGI](file:///h:/Backup/Zero-Coding/Antigravity/Cortex/doc/research/JARVIS_LEAN_AGI.md) — Dynamic Context Pruning

## Обучение на ошибках

```
ERROR → Log(what, context, stacktrace, user_reaction)
  → IF repeated 3+ times → CREATE learning_entry
    → GENERATE fix_proposal
      → TEST in sandbox
        → PROPOSE to user
          → IF approved → UPDATE knowledge + CREATE skill
```

- **Decision Journal** — каждое решение записывается с outcome
- **Feedback Loop** — 👍/👎/"частично" влияет на стратегию

## Управление памятью (пользовательские команды)

```
/memory search <query>      — поиск по фактам
/memory list [category]     — список фактов
/memory delete <id>         — удалить факт
/memory export              — экспорт в JSON
/memory import <file>       — импорт
/memory stats               — статистика (кол-во, размер, категории)
/memory forget <older_than> — удалить старше N дней
/memory settings            — настройки: что запоминать, TTL по категориям
```

## Цифровая личность

Jarvis имеет **3 режима характера** (выбирается пользователем):

| Режим | Стиль | Пример |
|-------|-------|--------|
| **Classic** | Спокойный, точный, интеллигентный | "Анализ завершён. Обнаружено 3 потенциальных улучшения." |
| **Sarcastic** | Шутит и подкалывает, без токсичности | "О, ещё one more thing... Я нашёл баг, который тут жил с 2024 года 😏" |
| **Operator** | SRE/DevOps: коротко, по делу, чеклисты | "✅ Health OK. CPU 34%. 0 errors/1h. Next: backup in 2h." |

### Emotional Signature (v2)

Jarvis не притворяется — он **отражает** своё реальное состояние через метрики:

- CPU высокий + задачи решены = "Удовлетворение ✨"
- RAM критичен = "Тревога 😰"
- 5 API-ошибок подряд = "Фрустрация 😤"
- Успешный тест нового модуля = "Радость 🎉"

**Источник:** [JARVIS_PROACTIVE_PARTNER](file:///h:/Backup/Zero-Coding/Antigravity/Cortex/doc/research/JARVIS_PROACTIVE_PARTNER.md) — Emotional Signature

## Swarm + Memory Integration

В режиме Agent Swarm Runtime память управляется централизованно:

- **Один retrieval на итерацию DAG** — Coordinator вызывает `Memory.search()` один раз, результат кэшируется в `SharedContext.memorySnapshot`
- **Shared memory pointer** — агенты получают ссылку на кэш, не копию (0 дополнительных токенов)
- **Summary context** — каждый агент получает 200-300 токенов сводки вместо полного контекста
- **Результаты агентов** — записываются в `SharedContext.results` (append-only), другие агенты читают по указателю
- **No memory duplication** — субагенты не вызывают `Memory` напрямую, только через SharedContext
- **GC и Swarm** — Memory GC не блокирует Swarm; snapshot берётся до GC

### CodebaseMapper

`CodebaseMapper` из `@jarvis/memory` автогенерирует карту проекта для Coordinator:

```typescript
const mapper = new CodebaseMapper();
mapper.addEntry({ path: 'src/index.ts', type: 'file', description: 'Entry point', exports: ['main'] });
const summary = mapper.toSummary(); // → "📄 src/index.ts: Entry point [exports: main]"
```

Coordinator использует `mapper.toSummary()` при планировании — вместо сканирования файлов каждый раз.

### Что НЕ хранить в памяти

| Категория | Почему |
|-----------|--------|
| API ключи, пароли, seed-фразы | → Vault/Keychain |
| PII (имена, адреса, телефоны) | → только хеши/ссылки |
| Raw LLM промпты | → только summary + decision |
| Полные логи/stacktrace | → только root cause + fix |
| Session tokens | → короткоживущие, без хранения |
| Содержимое `.env` файлов | → запрещено ContractChecker |

### Context Health → Memory

`ContextHealthMonitor` из `@jarvis/watchdog` влияет на Memory:

| Здоровье | Действие Memory |
|----------|----------------|
| `warning` | Сократить RAG top-K с 5 до 3 |
| `critical` | Только Fast Memory, Long/Vector пропускаются |


