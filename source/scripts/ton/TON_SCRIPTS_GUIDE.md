# 💎 Jarvis 5.1 NEXUS — TON Blockchain Scripts

> Набор надежных скриптов для реальных финансовых операций в сети TON (Mainnet).
> Поддержка Wallet V5R1 · Защита от двойных трат · Авто-подтверждение

---

## 🛠 Установка зависимостей

Перед запуском убедитесь, что в корне проекта установлены необходимые библиотеки:

```bash
cd /root/openclaw
npm install @ton/ton @ton/core @ton/crypto @ston-fi/sdk
```

---

## 📋 Описание скриптов

### 1. `stake_test.js` — Ликвидный стейкинг (Tonstakers)
Отправляет 1.0 TON на смарт-контракт Tonstakers для получения протокола ликвидного стейкинга.

- **Адрес получателя:** `EQDnHy-nxYFgUqzfUzImBEP67JqsyMIcyk2S5_RwNNEYku0k` (Tonstakers)
- **Газ:** Требует ~0.05 TON сверху на выполнение.
- **Логика:** Проверяет seqno кошелька до и после отправки для подтверждения блокчейном.

### 2. `swap_test.js` — Обмен TON → USDT (STON.fi)
Безопасно меняет 1.0 TON на USDT через DEX-роутер STON.fi v1.

- **DEX:** STON.fi v1 Router.
- **Slippage:** 5% (защита от резких изменений цены).
- **Газ:** Требует ~0.3 TON на комплексную DEX-операцию.
- **Идемпотентность:** Использует уникальный `queryId` для предотвращения дублирования транзакций.

---

## 🚀 Запуск скриптов

### Шаг 1: Настройка переменных окружения
Для безопасности мнемоника передается только через переменную окружения (в RAM), никогда не записывайте её в файлы.

```bash
# 24 слова вашего кошелька (Wallet V5R1)
export TON_MNEMONIC="word1 word2 ... word24"

# Опционально: API ключ от toncenter.com для обхода лимитов
export TONCENTER_API_KEY="your-api-key"
```

### Шаг 2: Выполнение
```bash
# Стейкинг
node scripts/ton/stake_test.js

# Обмен
node scripts/ton/swap_test.js
```

---

## 🛡 Безопасность и Гарантии

1. **Wallet V5R1:** Скрипты используют новейшую версию кошелька (WalletContractV5R1).
2. **Seqno Logic:** Транзакции не отправляются, если кошелек не готов, и ожидают реального изменения `seqno` в блокчейне для подтверждения успеха.
3. **Double Spends:** Механизм `seqno` и уникальных `queryId` гарантирует, что каждая команда будет исполнена ровно один раз.
4. **Прозрачность:** По завершении выводится прямая ссылка на `tonviewer.com` для визуального контроля.

---
⚠️ **ВНИМАНИЕ:** Скрипты работают в **Mainnet** с реальными активами. Всегда проверяйте баланс и адреса перед запуском.

⚠️ **ВАЖНО:** Оба скрипта работают с реальными деньгами на **mainnet**. Перед запуском проверь адреса и суммы. Мнемонику передавай **ТОЛЬКО через export**, никогда не хардкодь.
🦾 *Jarvis 5.1 NEXUS Asset Management Layer*
